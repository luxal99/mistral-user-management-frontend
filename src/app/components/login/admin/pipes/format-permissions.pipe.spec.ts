import { FormatPermissionsPipe } from './format-permissions.pipe';

describe('FormatPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
